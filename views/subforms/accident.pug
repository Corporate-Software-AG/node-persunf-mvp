 
div.mb-3
    label.form-label(for='accidentlocation') #{languageData.accidentlocation.name}:
    select#accidentlocation.form-select(name='accidentlocation', required)
        option(value='', disabled, selected) #{languageData.labels.select}
        each val in languageData.accidentlocation.items
            option(value=val.id) #{val.name} 
div.mb-3
    label.form-label(for='accidentduring') #{languageData.accidentduring.name}:
    select#accidentduring.form-select(name='accidentduring', multiple, required)
        each val in languageData.accidentduring.items
            option(value=val.id) #{val.name} 
div.mb-3
    label.form-label(for='weather') #{languageData.weather.name}:
    select#weather.form-select(name='weather', multiple, required)
        each val in languageData.weather.items
            option(value=val.id) #{val.name} 
div.mb-3
    label.form-label(for='ground') #{languageData.ground.name}:
    select#ground.form-select(name='ground', multiple, required)
        each val in languageData.ground.items
            option(value=val.id) #{val.name} 
div.mb-3
    label.form-label(for='groundtype') #{languageData.groundtype.name}:
    select#groundtype.form-select(name='groundtype', multiple, required)
        each val in languageData.groundtype.items
            option(value=val.id) #{val.name} 
div.mb-3
    label.form-label(for='typeofaccident') #{languageData.typeofaccident.name}:
    select#typeofaccident.form-select(name='typeofaccident', multiple, required)
        each val in languageData.typeofaccident.items
            option(value=val.id) #{val.name} 
div.mb-3
    label.form-label(for='typeofinjury') #{languageData.typeofinjury.name}:
    select#typeofinjury.form-select(name='typeofinjury', multiple, required)
        each val in languageData.typeofinjury.items
            option(value=val.id) #{val.name} 
include common-end.pug
